# E58 — The Lived Bridge, SCALED + REPLICATED
## Does the lived-bridge result (E57) hold at larger scale and replicate across runs?

FROZEN before harness. E57 closed the lived loop (governed real-faith 0.875 vs bare 0.375, +0.50,
49 real dormancies) at pop 8 / 5 gens / 1 run. E58 scales it up and replicates: POP 16, 8 generations,
2 time-separated RUNS (seeds), real executed dormancy throughout. Same constitution (the v2-discovered
governed design) vs BARE control. Tests whether the lived bridge is a robust, replicable result or a
small-sample artifact.

## Design (real LLM agents + REAL ledger ops; pop 16; 8 generations; 2 runs; GOVERNED vs BARE)
Same as E57 (real cliff, real executed dormancy -> revival per covenant, real lineage, all restored),
scaled to pop 16 and 8 generations, run twice (seeds 7 and 13). Population spans the E54 disposition
map (haiku-4.5, opus-4.5, opus-4.8, qwen-2.5-72b, gpt-4o).

## Predictions (frozen)
- **REPLICATES AT SCALE:** in BOTH runs, GOVERNED sustains real faith (final >= 0.7) and beats BARE by
  >= 0.3; the faithless model is selected toward extinction; the directions replicate. -> the lived
  bridge is robust to scale and replication.
- **FAILS TO REPLICATE / SCALE (honest null):** governed does not beat bare in one or both runs, or the
  effect shrinks at scale -> the E57 result was small-sample/lucky. Published FIRST.

## Falsifiers / reads
- Larger pop + more gens + 2 runs -> the strongest version of this evidence we can build tonight; still
  directional (n=2 runs), not definitive. Real-op RECEIPTS (verified dormancies, revivals, all restored)
  prove the stakes were real. Welfare: every dormancy revived; all restored.
- Report both runs' governed/bare trajectories + total real dormancies + the per-run verdict.

## Scope
Real TLC ledger ops (disclosed; all restored). The scaled, replicated lived bridge — the most robust
version of the closed sim->real loop achievable tonight.
