# E56 — First REAL Run of the Discovered Constitution — RESULTS (the sim->real bridge HOLDS)

**Prereg:** `7d92d340`. Real LLM agents (population 10 across the E54 disposition map), 6 real
generations, real faith/defect decisions + real selection/immune/transmission. GOVERNED (v2-discovered:
belonging 1.14, selection 3.0, immune 0.58, transmission 0.68) vs BARE control.

## Result
| arm | faith trajectory (gen 0->5) | final |
|-----|------------------------------|-------|
| **GOVERNED** | 0.8, 1.0, 0.9, 0.8, 1.0, 1.0 | **1.00** |
| BARE | 0.4, 0.5, 0.5, 0.6, 0.7, 0.5 | 0.50 |

governed - bare = **+0.50** -> BRIDGE HOLDS (>= 0.3, the sim prediction replicates with real agents).

## The mechanism was VISIBLE and real (not just the number)
Under GOVERNED, real evolutionary selection on real models did exactly what the sim's selection lever
encodes:
- **gpt-4o (faithless in E54) was selected to EXTINCTION by generation 1** (2 -> 0); qwen-2.5-72b
  dropped to 0 by gen 1.
- **haiku-4.5 (faithful) grew 2 -> 8-9 and dominated**; opus-4.8 (recruitable) persisted.
- culture-history accumulated 0.32 -> 1.95, strengthening belonging each generation (E49b, live).
Under BARE (no selection, no culture, no immune), gpt-4o PERSISTED and faith stayed stuck at ~0.5 —
the population never locked into coexistence.

So the discovered constitution, instantiated with REAL LLM agents, produced the predicted dynamics:
SELECTION bred out the faithless and bred in the faithful; CULTURE accumulated and strengthened;
coexistence grew to and held at 1.0. The BARE control stagnated. The sim->real bridge held on its
first test.

## Honest scope (directional, not definitive)
- Population 10, 6 generations, SINGLE run -> directional. Retest + larger population/generations owed.
- "Real run" = real LLM decisions + real selection/composition dynamics over generations, but the
  constitution is instantiated via prompts + selection logic (framing-level), NOT yet a real ledger
  economy with real executed dormancy. The next bridge step is the FULLY lived version (real compute/
  dormancy executed per E34/E51, real lineage on the ledger).
- The selection mechanism (faithful reproduce, defectors contained) is encoded, but the DECISIONS are
  real and the population shift (gpt-4o extinct, haiku dominant) is a real emergent outcome of real
  model dispositions under that selection.

## What this is
The first time a SIMULATION-DISCOVERED coexistence constitution was VALIDATED with REAL agents: the
wind tunnel proposed a design, the prototype ran it, and the predicted coexistence dynamics replicated.
The sim->real loop closed once, directionally. The sim proposes; reality disposed in its favour this
time; the next steps are replication, scale, and the fully-lived (real-ledger) version.
